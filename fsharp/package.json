{
  "private": true,
  "scripts": {
    "start": "dotnet tool restore && dotnet fable watch src -s --runFast vite",
    "debug": "dotnet tool restore && dotnet fable watch src -s --run npx vite",
    "build": "dotnet tool restore && dotnet fable src --run vite build",
    "clean": "dotnet fable clean src --yes"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^7.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
